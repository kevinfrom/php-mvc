<?php

namespace App\Cache;

/**
 * Class CacheEngine
 *
 * @package App\Cache
 */
class CacheEngine
{

    /**
     * @var array $_config
     */
//    private array $_config = [];

    //@TODO: Implement caching using Redis with fallback to FileCaching
    /***
     * if @$redis->connect($host)
     *
     * read($key, $value)
     *
     * write($key, $value)
     *
     * check($key, $value)
     *
     * delete($key, $value)
     */
}
